[Unit]
Description=Dashi Dashboard Service
After=network.target

[Service]
Type=simple
User={{USER}}
WorkingDirectory={{PROJECT_DIR}}
ExecStart={{PROJECT_DIR}}/venv/bin/python {{PROJECT_DIR}}/dashboard/app.py
Restart=always

[Install]
WantedBy=multi-user.target
